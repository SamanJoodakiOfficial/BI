<!DOCTYPE html>
<html lang="fa" dir="rtl">
  <%- include("../partials/head") %>

  <body class="bg-primaryBackground">
    <div class="w-full min-h-screen container flex items-center justify-center">
      <form
        action="/auth/register"
        method="post"
        class="w-[400px] max-w-[400px] p-4 bg-white rounded-lg shadow-lg flex flex-col items-center justify-between gap-4"
      >
        <h2 class="font-bold text-lg text-202020">ثبت نام حساب کاربری</h2>

        <!-- Errors -->
        <% var errors = errors || '' %> <% if (errors.length >= 1) { %> <%
        errors.forEach((error) => { %>
        <div
          class="w-full min-h-8 rounded-lg bg-error-light-1 text-error text-sm flex items-center p-4 gap-1"
        >
          <i class="ri-error-warning-line"></i>
          <%= error.msg %>
        </div>
        <% }) %> <% } %>
        <!-- Error -->
        <% if (errorMessage && errorMessage.length > 0) { %>
        <div
          class="w-full min-h-8 rounded-lg mt-4 bg-error-light-1 text-error text-sm flex items-center p-4 gap-1"
        >
          <i class="ri-error-warning-line"></i>
          <%= errorMessage %>
        </div>
        <% } %>

        <!--Success-->
        <% if (successMessage && successMessage.length > 0) { %>
        <div
          class="w-full min-h-8 rounded-lg bg-success-light-1 text-white text-sm flex items-center p-4 gap-1"
        >
          <i class="ri-error-warning-line"></i>
          <%= successMessage %>
        </div>
        <% } %>

        <div class="w-full flex flex-col items-center gap-4 m-4">
          <div class="w-full flex flex-col gap-1">
            <label for="email" class="text-404040 text-xs">ایمیل کاربری</label>
            <input
              class="w-full h-12 border rounded-lg text-606060 bg-primaryBackground focus:ring-2 focus:ring-primary text-center outline-none px-4"
              type="email"
              name="email"
              id="email"
              placeholder="آدرس ایمیل"
              value="<% var filled = filled || '' %><%= filled.email %>"
              required
            />
          </div>

          <div class="w-full flex flex-col gap-1 relative">
            <label for="password" class="text-404040 text-xs">کلمه عبور</label>
            <input
              class="w-full h-12 border rounded-lg border-CBCBCB text-606060 bg-primaryBackground focus:ring-2 focus:ring-primary text-center outline-none px-4"
              type="password"
              name="password"
              id="password"
              placeholder="کلمه عبور"
              required
            />
          </div>

          <div class="w-full flex flex-col gap-1 relative">
            <label for="confirmPassword" class="text-404040 text-xs"
              >تایید کلمه عبور</label
            >
            <input
              class="w-full h-12 border rounded-lg border-CBCBCB text-606060 bg-primaryBackground focus:ring-2 focus:ring-primary text-center outline-none px-4"
              type="password"
              name="confirmPassword"
              id="confirmPassword"
              placeholder="تایید کلمه عبور"
              required
            />
          </div>
        </div>

        <button
          type="submit"
          class="w-full h-10 rounded-lg bg-primary text-white hover:-translate-y-1 transition-all shadow-lg shadow-primary/30 hover:shadow-xl"
        >
          ثبت نام
        </button>
        <span class="text-xs text-202020"
          >حساب کاربری دارید؟
          <a href="/auth/login" class="text-primary underline"
            >وارد شوید</a
          ></span
        >
      </form>
    </div>
  </body>
</html>
